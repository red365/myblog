<h2>Tags</h2>

<% @tags.each do |tag| %>
  <p>
    <%= link_to tag.name, tag_path(tag.id) %> <%= link_to 'Delete', tag_path(tag.id),
						 method: :delete,
						 data: { confirm: 'Are you sure?' } %>
  </p>
  <% end %>
